services:
  bienchen:
    image: nginx:1.27-alpine
    container_name: bienchen
    ports:
      - "8099:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
